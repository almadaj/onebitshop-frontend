import React from "react";
import { Container, ErrorTitle } from "./styles";

const ErrorScreen = () => {
  return (
    <Container>
      <ErrorTitle>Está tela teve algum erro</ErrorTitle>
    </Container>
  );
};

export default ErrorScreen;
